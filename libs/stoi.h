#ifndef __STOI_H__
#define __STOI_H__

#include <stdio.h>
#include <ctype.h>  // for isdigit
#include <stdlib.h> // for strtol

/* =========={ STOI - STRING TO INTEGER }=============================
   Take an array of characters as a number, return its integer
   equivalent.
   ================================================================ */
int stoi( char string[] );

#endif
